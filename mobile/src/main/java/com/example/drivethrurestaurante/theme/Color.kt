package com.example.drivethrurestaurante.theme

import androidx.compose.ui.graphics.Color

// Paleta de colores basada en la imagen del menú
val RedPrimary = Color(0xFFE85A5A)       // Rojo coral de los círculos del menú
val RedDark = Color(0xFFD44444)          // Versión más oscura para pressed states
val RedLight = Color(0xFFF06E6E)         // Versión más clara para hover/focus

val Background = Color(0xFFF8F9FA)       // Fondo claro
val SurfaceLight = Color(0xFFFFFFFF)     // Tarjetas y áreas blancas
val SurfaceGray = Color(0xFFF5F5F5)      // Fondo de secciones alternativas

val TextPrimary = Color(0xFF212529)      // Texto oscuro principal
val TextSecondary = Color(0xFF6C757D)    // Texto gris secundario
val TextLight = Color(0xFF9CA3AF)        // Texto más claro para descripciones

val IconGray = Color(0xFFADB5BD)         // Iconos no activos
val IconActive = Color(0xFF4F46E5)       // Iconos activos (azul)

// Colores para estados
val Success = Color(0xFF10B981)          // Verde para éxito
val Warning = Color(0xFFF59E0B)          // Amarillo para advertencias
val Error = Color(0xFFEF4444)            // Rojo para errores

// Colores específicos para la UI del drive thru
val CardBackground = Color(0xFFFFFFFF)   // Fondo de tarjetas de productos
val DividerColor = Color(0xFFE5E7EB)     // Líneas divisorias
val BadgeBackground = Color(0xFFE85A5A)  // Fondo de badges/contadores
val BadgeText = Color(0xFFFFFFFF)        // Texto en badges